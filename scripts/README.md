# scripts/

One-off and operational scripts: ingest a corpus, build an index, run an eval
set, export annotations. Scripts should be thin wrappers around library code in
`src/ragplatform/` so that all logic stays tested.
